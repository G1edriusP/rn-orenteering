import React from "react";

// Styles
import styles from "containers/Home/TracksMap";

// Components
import { View, Text } from "react-native";

// Constants
import { TracksMapScreenProps } from "constants/navigation/types";

// Utils

// Other

const TracksMap = ({ navigation }: TracksMapScreenProps) => {
  return (
    <View>
      <Text>Map</Text>
    </View>
  );
};

export default TracksMap;
